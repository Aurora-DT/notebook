# 桌面记事本软件 - 项目文档

> 基于 Electron + TypeScript 的 Windows 本地桌面记事本应用

## 一、项目概述

### 1.1 项目简介
一款轻量级、可常驻桌面、支持置顶与侧边栏停靠的本地记事本软件，面向需要快速记录、随手备忘、临时贴板等场景的用户。

### 1.2 目标平台
- Windows 10 / 11（首要）
- 后续可扩展 macOS、Linux

### 1.3 技术栈
| 层级 | 技术 | 说明 |
| --- | --- | --- |
| 运行时 | Node.js 24 | 已安装 |
| 桌面框架 | Electron | 跨平台桌面壳 |
| 语言 | TypeScript | 主从语言 |
| UI 渲染 | Vue 3 + Vite | 组合式 API，单文件组件 |
| 构建 | Vite + electron-builder | 打包成 Windows 安装包 |
| 状态/数据 | 本地 JSON 文件（lowdb / 自封装） | 无需数据库依赖 |
| 前端状态 | Pinia | Vue 3 官方推荐状态管理 |
| 编辑器 | CodeMirror 6（推荐） | 支持 Markdown / 代码高亮 / 查找替换 |
| 代码规范 | ESLint + Prettier | 统一代码风格 |

## 二、功能需求

### 2.1 核心需求（用户明确要求）

#### F1. 窗口置顶
- 支持一键将窗口固定在桌面最前方
- 通过工具栏按钮、快捷键、托盘菜单三种方式切换
- 置顶状态应有视觉指示（图标高亮 / 标题栏标识）
- 置顶状态在重启后保持

#### F2. 侧边栏停靠与收缩
- 侧边栏默认停靠在窗口左侧，显示笔记列表
- 支持以下交互：
  - 点击按钮收缩/展开（宽度过渡动画）
  - 拖拽边缘调整宽度（最小/最大宽度限制）
  - 收缩后仅显示图标条，鼠标悬停可预览
- 收缩状态记忆（重启后保持）

#### F3. 快捷键操作
- 全局快捷键（应用未激活也可触发）：
  - `Ctrl+Shift+N`：唤起/隐藏主窗口
  - `Ctrl+Shift+T`：新建临时笔记
- 应用内快捷键：
  - `Ctrl+N`：新建笔记
  - `Ctrl+S`：保存（自动保存开启时为强制保存）
  - `Ctrl+F`：查找
  - `Ctrl+H`：替换
  - `Ctrl+P`：打印
  - `Ctrl+B/I/U`：粗体/斜体/下划线
  - `Ctrl+D`：删除当前笔记
  - `Ctrl+E`：聚焦搜索框
  - `Ctrl+,`：打开设置
  - `F11`：全屏切换
  - `Esc`：关闭弹层/退出全屏
- 快捷键可在设置中自定义（MVP 可暂缓）

### 2.2 推荐补充功能

#### F4. 笔记管理
- 多笔记 Tab 切换
- 笔记分组/文件夹
- 标签系统（多标签归类）
- 收藏/置顶笔记
- 回收站（删除后可恢复）

#### F5. 自动保存
- 输入停止 N 秒后自动保存（默认 2 秒）
- 标题栏显示「已保存 / 保存中 / 未保存」状态
- 异常退出后可恢复未保存内容

#### F6. 编辑器增强
- 纯文本 + Markdown 双模式
- 行号显示（可选开关）
- 字数/字符统计
- 自动缩进、智能括号配对
- 撤销/重做（Ctrl+Z / Ctrl+Y）
- 查找替换（正则支持）

#### F7. 搜索
- 全局搜索：跨所有笔记全文检索
- 高亮匹配关键词
- 支持按标签、文件夹、日期范围筛选

#### F8. 主题与外观
- 浅色 / 深色 / 跟随系统
- 字体、字号、行距可调
- 编辑区背景色自定义
- 透明度调节（窗口整体半透明，方便对照参考）

#### F9. 系统托盘
- 最小化到托盘
- 托盘菜单：新建笔记、显示窗口、退出
- 开机自启（可选）

#### F10. 导入导出
- 导出：TXT / Markdown / HTML / PDF
- 导入：TXT / Markdown
- 批量导出为 ZIP

#### F11. 数据持久化与备份
- 本地 SQLite 存储笔记、标签、配置
- 自动定时备份（每日/每周）
- 手动导出备份文件
- 数据迁移路径可配置

#### F12. 历史版本
- 笔记每次保存生成一个版本快照
- 可查看与恢复历史版本
- 自动清理超过 N 天的旧版本

#### F13. 多窗口支持
- 主窗口承载侧边栏 + 笔记列表 + 主编辑区
- 任意笔记可「在新窗口打开」，独立成一个可拖拽、可置顶的浮动窗口
- 各窗口状态独立（置顶、大小、位置），但共享同一份数据源
- 关闭主窗口时可选择：退出应用 / 隐藏到托盘（独立笔记窗口不退出）
- 多窗口间数据变更通过 IPC 实时同步（一窗口修改 → 其他窗口刷新）
- 窗口管理器统一维护窗口列表，支持「全部置顶」「全部收起」等批量操作

### 2.3 后期可扩展功能（非本期）
- 多设备同步（WebDAV / 坚果云 / Git）
- 端到端加密笔记
- 富文本图片插入
- OCR 截图识图插入
- 插件系统
- 代码片段高亮管理

## 三、架构设计

### 3.1 进程划分
```
┌──────────────────────────────────────────┐
│  Main Process (主进程)                    │
│  - 创建窗口、管理生命周期（主窗口+多笔记窗口）│
│  - 全局快捷键注册                          │
│  - 系统托盘                                │
│  - 文件 I/O、JSON 数据读写                 │
│  - 多窗口间数据变更广播                    │
│  - IPC 调度                                │
└──────────────┬───────────────────────────┘
               │ IPC (contextBridge)
┌──────────────┴───────────────────────────┐
│  Renderer Process (渲染进程)              │
│  - UI 渲染                                 │
│  - 编辑器                                  │
│  - 用户交互                                │
└──────────────────────────────────────────┘
```

### 3.2 项目目录结构（建议）
```
vibecoding/
├── package.json
├── tsconfig.json
├── vite.config.ts
├── electron-builder.yml
├── .eslintrc.cjs
├── .prettierrc
├── .gitignore
├── docs/
│   └── PROJECT_DOC.md
├── src/
│   ├── main/                    # 主进程
│   │   ├── index.ts             # 入口
│   │   ├── window.ts            # 主窗口管理
│   │   ├── note-window.ts       # 独立笔记窗口管理（多窗口）
│   │   ├── window-manager.ts    # 窗口注册表 + 广播
│   │   ├── tray.ts              # 托盘
│   │   ├── shortcut.ts          # 全局快捷键
│   │   └── ipc/                 # IPC 处理器
│   ├── preload/                 # 预加载脚本
│   │   └── index.ts
│   ├── renderer/                # 渲染进程（Vue 3）
│   │   ├── index.html
│   │   ├── src/
│   │   │   ├── main.ts          # Vue 应用入口
│   │   │   ├── App.vue          # 根组件
│   │   │   ├── components/      # 通用组件（.vue）
│   │   │   ├── views/           # 页面（主窗口/笔记窗口/设置）
│   │   │   ├── stores/          # Pinia 状态管理
│   │   │   ├── services/        # 调用 IPC 的封装
│   │   │   ├── composables/     # 组合式函数
│   │   │   ├── utils/
│   │   │   └── assets/
│   │   └── tsconfig.json
│   ├── shared/                  # 主/渲染共享类型
│   │   └── types.ts
│   └── db/                      # 数据层（JSON）
│       ├── store.ts             # lowdb 实例 / 文件读写
│       └── repository.ts        # 笔记 CRUD
└── resources/                   # 图标、静态资源
```

### 3.3 关键技术点
- **窗口置顶**：`BrowserWindow.setAlwaysOnTop(true, 'screen-saver')`
- **侧边栏收缩**：CSS `transition` + 状态持久化到本地配置
- **全局快捷键**：`globalShortcut.register()` 在主进程注册
- **自动保存**：渲染进程 debounce → IPC → 主进程写入 JSON
- **数据存储**：`app.getPath('userData')` 下建立 `notes.json`（lowdb）
- **多窗口**：主进程维护 `Map<noteId, BrowserWindow>`，笔记变更后通过 `webContents.send()` 向所有打开该笔记的窗口广播更新
- **编辑器集成**：CodeMirror 6 通过 npm 引入，在 Vue 组件中用 `onMounted` 初始化实例，按需加载 Markdown 语言包
- **状态管理**：Pinia 管理 UI 状态（侧边栏、置顶、当前笔记等），IPC 数据通过 service 层注入 store

## 四、UI / UX 设计要点

### 4.1 主界面布局
```
┌────────────────────────────────────────────┐
│ 标题栏  [置顶▣] [—] [□] [×]                │
├──────┬─────────────────────────────────────┤
│ 侧边 │ 工具栏 [新建][保存][查找][...]      │
│ 栏   ├─────────────────────────────────────┤
│      │                                     │
│ 笔记 │       编辑区                        │
│ 列表 │                                     │
│      │                                     │
│ [+]  │ 字数：123 · 已保存 14:30            │
└──────┴─────────────────────────────────────┘
```

### 4.2 交互原则
- 启动到首屏 < 1 秒
- 所有操作均可键盘完成
- 关键操作（删除、覆盖）需二次确认
- 状态可见：保存状态、置顶状态、网络状态（如启用同步）

## 五、开发计划（分阶段）

### 阶段 1：MVP（最小可用）
- 项目脚手架搭建（Vue 3 + Vite + Electron + TS）
- 多窗口管理基础（主窗口 + 独立笔记窗口）
- 集成 CodeMirror 6 编辑器（Vue 组件封装）
- 本地 JSON 文件保存
- 窗口置顶（F1）
- 侧边栏收缩（F2）
- 基础快捷键（F3 应用内）
- 自动保存（F5）

### 阶段 2：笔记管理
- 多笔记 + Tab（F4）
- 全局搜索（F7）
- 系统托盘（F9）
- 导入导出 TXT/MD（F10）

### 阶段 3：体验完善
- 主题系统（F8）
- 历史版本（F12）
- 全局快捷键唤起（F3 全局）
- 数据备份（F11）

### 阶段 4：扩展能力
- 同步、加密、插件等（2.3）

## 六、配置与环境

### 6.1 开发依赖
- electron
- electron-builder
- typescript
- vite
- vue
- pinia
- eslint / prettier
- lowdb

### 6.2 运行命令（预期）
```bash
npm run dev          # 启动开发
npm run build        # 构建产物
npm run package      # 打包可执行文件
npm run lint         # 代码检查
```

## 七、已确认决策
| # | 决策项 | 选择 | 备注 |
| --- | --- | --- | --- |
| 1 | UI 框架 | Vue 3 + Vite | 组合式 API，单文件组件，搭配 Pinia 状态管理 |
| 2 | 数据存储 | 本地 JSON 文件 | 使用 lowdb 或自封装，无数据库依赖 |
| 3 | 账号体系 | 无 | 纯本地工具，不涉及登录 |
| 4 | 编辑器 | 集成 CodeMirror 6 | 支持 Markdown、代码高亮、查找替换 |
| 5 | 多窗口 | 支持 | 主窗口 + 独立笔记浮动窗口（见 F13） |

---

> 文档版本：v0.3（UI 框架改为 Vue 3）
> 更新日期：2026-07-31
